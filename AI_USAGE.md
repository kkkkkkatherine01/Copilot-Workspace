# AI 协作记录

本项目通过 Claude Code 分阶段协作完成（先出 Spec 和分阶段实施计划，再按"检索→生成→接口→前端
→加分项→文档"的顺序逐步实现并验证，过程见 `IMPLEMENTATION_PLAN.txt`）。这里记录人机分工，
方便面试时说明每部分的来源和决策依据。

## 协作方式

- 架构设计、技术选型等关键决策由人工确认：AI 提出方案和权衡，我逐项确认或推翻，不是 AI 自主决定后直接执行
- 代码主要由 AI 编写，但每个阶段完成后都会实际运行验证，而不是"看起来对就跳过"——包括真实跑
  `eval.py` 看检索准确率、真实调用 Claude API 观察生成效果、在浏览器里手动点三个按钮确认反馈闭环、
  查询 SQLite 确认数据真的落库了
- 过程中多次对 AI 的输出提出纠正，具体见下

## 关键的人工决策点

1. **技术栈从 Streamlit 改为 FastAPI + React**：最初的 Spec 定的是 Streamlit 单体应用，
   讨论中确认题目要求"含前后端"，要求改为真正的前后端分离架构
2. **embedding 模型从 `all-MiniLM-L6-v2` 改为中文模型**：核对 Spec 时发现原定模型主要面向英文语料，
   对中文 FAQ 场景不合适，讨论后确定改用 `shibing624/text2vec-base-chinese`
3. **部署方案从 Render 改为 Railway**：AI 调研后发现 Render 免费层是 ephemeral filesystem，
   服务休眠/重启会清空 SQLite 反馈日志，改为 Railway + Persistent Volume
4. **纠正"必须先选测试数据才能对话"的设计偏差**：AI 最初把测试场景下拉框做成了对话的前置条件，
   被指出不符合"客服与用户对话、实时生成"的题意后，改成完全自由输入，测试场景只作为可选的演示起点
5. **UI 重做**：初版界面能用但比较简陋（输入框随内容增长往下掉、没有聊天气泡区分角色），
   要求重做成固定高度布局、消息区内部滚动、聊天气泡样式的界面
6. **对"已经真实调用 Claude API"这一说法的核实**：后台一度看不到用量记录，产生怀疑，
   要求 AI 用 API 返回的 message id、token 用量等没法伪造的原始元数据现场验证，
   最终确认是账号 workspace 看错了

## AI 独立完成、经人工验证通过的部分

- `retrieval.py` 的编码策略调优（FAQ 的 keywords 加权两次），是先跑 `eval.py` 看到准确率
  从 60% 提升到 80% 且没有破坏已命中的case，才确认采纳，不是凭感觉调的
- `generation.py` 里"低置信度时不调用LLM、直接走确定性兜底文案"的设计
- Prompt V1→V2 的迭代，V2 的每一条改进都对应 V1 真实输出里观察到的具体问题（啰嗦、markdown格式、
  不会代入具体数字），不是凭空假设
- `BAD_CASE_ANALYSIS.md` 里的两个case，都是从 `eval.py` 和手动测试的真实输出里挑出来的

## 未尽事项 / 留给后续的判断

- 检索阶段目前不感知对话历史（只有生成阶段感知），如果要做需要评估检索 query 改写的复杂度和延迟成本，
  当前判断是"生成层兜底基本够用，投入产出比不高"，但这是一个可以重新评估的取舍
- 部署上线后中国大陆网络的实际可达性还没有在真实环境里验证过，目前只能靠录屏兜底
