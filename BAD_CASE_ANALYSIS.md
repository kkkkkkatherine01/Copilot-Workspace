# Bad Case 分析

以下两个 case 都是系统真实跑出来的问题（`backend/eval.py` 和 `backend/generation.py` 的实测输出），
不是假设的场景。

## Case 1：检索层——语义相近的多条FAQ互相"抢答"

**用户消息**：`我有个满200减30的券，买了一个180的东西能用吗？`（conv_003，`eval.py` 标注难度 medium）

**期望命中**：`faq_003`（优惠券怎么使用？）

**实际检索 top-3**（`eval.py` 输出）：

```
top1=faq_013 (score=0.599) 多张优惠券可以叠加使用吗？
top2=faq_014 (score=0.586) 优惠券过期了还能用吗？
top3=faq_003 (score=0.551) 优惠券怎么使用？   ← 正确答案排第3
```

**问题出在哪**：
faq_003/faq_013/faq_014 都是"优惠券"分类下的短问句，字面和语义都高度相似（都在问优惠券"能不能用"），
纯 embedding 相似度很难分辨"用户问的是门槛没达到"这种更细的语义差异，三条分数只差 0.01~0.05，
本质上是**检索层面的天花板**，不是简单调参能解决的。

**实际影响有多大**：因为生成模块用的是 top-3 而不是只用 top-1，Claude 在看到三条候选后自己正确
挑出了 faq_003 来组织答案（见 `PROMPT_ITERATION.md` Case 1，回复内容是对的，还算出了"还差20元"）。
所以**最终用户看到的回复是正确的**，只是 `eval.py` 这种"只看top-1"的评测口径会把它算成 MISS，
这也说明单独看检索准确率会低估系统的真实可用性。

**可能的改进方向**：
1. 检索之后加一个轻量的重排序（rerank）步骤，比如把 top-5 交给 LLM 做一次"这些FAQ里哪条最相关"的
   判断，而不是直接相信 embedding 的排序——但这样每次请求会多一次LLM调用，增加延迟和成本，需要权衡
2. 给 FAQ 补充更细粒度的 keywords（比如给 faq_003 加上"门槛不够"、"差多少钱"这类关键词），
   让关键词加权（当前策略）能更精准地区分这几条相似FAQ
3. 接受现状：既然生成层已经能从 top-3 里纠错，这个问题的优先级可以降低

## Case 2：架构限制——检索不感知对话历史，导致指代消解失败

**场景**：接着 Case 1 的对话，用户追问 `那如果我再买一件20块的凑够200，这张券还能用吗？`

**问题**：这句话单独拿出来看，"这张券"是什么、"200"指什么，都要靠上一轮对话才能确定。
但当前系统的检索模块（`retrieval.py` 的 `FaqIndex.retrieve`）只对**当前这一句**做 embedding
匹配，不知道有历史存在：

```
用户消息: 那如果我再买一件20块的凑够200，这张券还能用吗？
检索top1: faq_014 (score=0.579) 优惠券过期了还能用吗？   ← 同样不是最相关的
```

**和 Case 1 的本质区别**：Case 1 是"三条FAQ本身很像，排序排错了"；Case 2 是**检索模块的输入
信息不完整**——它甚至不知道用户在聊优惠券门槛这件事，只能从"这张券还能用吗"这几个字里去猜，
属于更根本的架构限制，不是靠调权重能解决的。

**实际影响有多大**：和 Case 1 类似，生成阶段因为拿到了对话历史（阶段8实现的多轮上下文感知），
即使检索给的候选不够精准，Claude 依然能靠历史正确回答"可以使用"（见 `PROMPT_ITERATION.md`
多轮追问对比）。但这是"生成层兜底"，不代表检索本身是对的——如果历史更长、更复杂，
生成层不一定每次都能靠自己纠正检索的偏差。

**可能的改进方向**：
1. 检索时把最近1-2轮的历史拼接到查询文本里再编码（比如 `历史用户消息 + 当前消息` 一起embedding），
   让检索也具备上下文感知，而不是只让生成层兜底
2. 更简单的做法：判断当前消息是否包含代词/过短（比如"这张""能用吗"这种指代性很强的短句），
   命中就自动把历史拼进检索query，不需要每次都拼（避免历史太长稀释掉当前问题的语义权重）
3. 这属于阶段8"多轮上下文感知"里明确划出去、没做的范围（见 `IMPLEMENTATION_PLAN.txt` 阶段8的
   "已知限制"说明），当前用生成层兜底是在时间预算内的合理取舍，但如果继续投入，这是下一步该做的事

### 更新：已实现方案2（启发式触发的query改写）

后续投入时间做了方案2：`retrieval.looks_context_dependent()` 判断当前消息是否短/含指代词
（"这""那""它""上面""刚才""之前"等），命中才触发 `generation.rewrite_query_with_history()`
用 Claude 把消息改写成一句独立完整的问题，再拿改写后的句子去检索（原始消息不变，仍然传给生成
阶段用于组织最终回复）。这样大多数单轮消息完全不受影响，不多花一次调用成本。

**改写前后对比**（同一句追问，重复测试3次）：

改写前（固定）：
```
faq_014 (0.579) 优惠券过期了还能用吗？
faq_013 (0.542) 多张优惠券可以叠加使用吗？
faq_029 (0.522) 会员到期后权益会怎样？未用完的权益能延期吗？   ← faq_003 完全不在候选里
```

改写后（3次实测，faq_003 每次都进了top-3，且都被LLM正确引用）：
```
trial1: faq_013(0.669) faq_003(0.645) faq_014(0.635)   引用: [faq_003]
trial2: faq_013(0.663) faq_003(0.638) faq_014(0.627)   引用: [faq_003]
trial3: faq_013(0.688) faq_014(0.652) faq_003(0.648)   引用: [faq_003]
```

**诚实说明局限**：faq_013 的分数仍然经常压过 faq_003 排在第一（这就是Case 1那个"三条优惠券FAQ
互相很像"的老问题，查询改写解决不了这个），但改写把 faq_003 从"完全检索不到"变成了"稳定进入
top-3"，这个提升是真实、可重复的。另外查询改写本身也是LLM调用，输出有一定随机性，不保证每次
改写的措辞完全一致，只是实测下来对这个case的最终效果比较稳定。
