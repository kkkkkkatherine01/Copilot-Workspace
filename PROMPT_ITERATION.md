# Prompt 迭代记录

生成模块（`backend/generation.py`）目前保留了两版 prompt（`PROMPT_TEMPLATE_V1` / `PROMPT_TEMPLATE_V2`），
可通过 `generate_suggestion(..., prompt_version="v1"|"v2")` 切换，默认使用 V2。

以下对比数据全部来自 `backend/generation.py` 的真实调用输出（Claude Haiku，`shibing624/text2vec-base-chinese`
检索结果），没有人工编造或润色。

> 补充说明：`generation.py` 里还有第三个 prompt——`QUERY_REWRITE_PROMPT`，用途是在检索之前把
> 依赖上下文的追问（比如"这张券还能用吗"）改写成独立完整的问题，跟这里对比的"生成客服回复"
> 是完全不同的用途。它只有一个版本，不存在A/B对比（改写任务本身比较机械，不像回复生成那样
> 需要反复调风格），所以没有放进这份迭代记录里。它的效果对比记录在
> [BAD_CASE_ANALYSIS.md](BAD_CASE_ANALYSIS.md) Case 2 里（改写前后检索结果的真实对比数据）。

## V1（初版）

```
你是电商平台的客服助手，任务是根据下面提供的FAQ知识库内容，为人工客服生成一条可以直接发给用户的回复建议。

严格遵守以下规则：
1. 只能使用下面提供的FAQ内容作答，不能编造FAQ中没有出现的具体信息（比如天数、金额、流程细节）。
2. 语气专业、友好，像真实客服说话，不要暴露"我是根据FAQ回答"这类内部逻辑。
3. 回复正文结束后另起一行，用固定格式标注本次参考的FAQ id，格式必须是：[参考: faq_001, faq_003]
   （如果参考了多条就都列出来，用英文逗号分隔；只能引用下面确实提供了的FAQ id）。

以下是检索到的相关FAQ：
{faq_context}
{history_section}
用户消息：{user_message}

请生成客服回复建议：
```

**实测发现的问题**：
1. 经常输出 markdown 加粗、分点列表（`**标题**`、`- 项目符号`），读起来不像真实客服聊天窗口里
   一句话回复的语气，更像一篇说明文档
2. 遇到需要计算/代入具体数字的场景时，有时只给通用规则，不会主动算出"还差多少"这种更有用的结论
3. 回复长度不受控制，简单问题也可能写出四五段

## V2（改进版，当前默认）

```
你是电商平台的客服助手，为人工客服生成一条可以直接发给用户的回复建议。

严格遵守以下规则：
1. 只能使用下面提供的FAQ内容作答，不能编造FAQ中没有出现的具体信息（天数、金额、流程细节等）。
2. 回复风格要求：
   - 开头用简短的致歉/感谢开场（如"您好"、"感谢您的耐心等待"），不要用"亲"这类过于口语化的称呼
   - 正文控制在3句话以内，直接给结论和下一步操作，不要用分点列表、不要用markdown加粗或标题
   - 不要复述用户的问题
3. 如果用户消息里包含具体的数字或场景（金额、时间等），必须把这些数字代入结论明确回答，
   不能只给通用规则（比如用户问"180元的东西能不能用满200的券"，要直接说明"还差多少元、能不能用"，
   而不是只说"达到门槛即可使用"）。
4. 回复正文结束后另起一行，用固定格式标注参考的FAQ id：[参考: faq_001, faq_003]
   （只能引用下面提供了的FAQ id，多条用英文逗号分隔）。

以下是检索到的相关FAQ：
{faq_context}
{history_section}
用户消息：{user_message}

请生成客服回复建议：
```

**针对性改动**：限定长度和格式、禁用markdown、强制把用户消息里的具体数字代入结论。

## 对比 Case

### Case 1：conv_003（medium）—— 优惠券门槛计算

用户消息：`我有个满200减30的券，买了一个180的东西能用吗？`

| | V1 | V2 |
|---|---|---|
| 输出 | 感谢您的提问！根据您的情况，这张满200减30的优惠券需要订单金额满足200元才能使用，而您的购买金额是180元，所以暂时无法使用这张优惠券。<br><br>建议您可以：<br>- 继续购物，凑满200元后再使用这张券<br>- 查看是否有其他满足条件的优惠券可用<br>- 关注平台后续活动，获取更多优惠券机会<br><br>如果您还有其他疑问，欢迎继续咨询！ | 您好，感谢咨询。您购买的商品金额为180元，而满200减30的优惠券需要订单满200元才能使用，所以目前还差20元无法达到使用门槛。建议您可以继续选购其他商品凑单至200元以上，这样就可以在结算页面使用该优惠券了。 |
| 差异 | 3段+3点罗列，其中"查看其他优惠券"、"关注活动"是FAQ里没直接支持的泛化建议 | 2句话说完，**明确算出"还差20元"**，比V1的结论更具体有用 |

### Case 2：conv_004（medium）—— 组合问题（换手机号+忘密码）

用户消息：`我换了手机号，现在登不上去了，密码也忘了`

| | V1 | V2 |
|---|---|---|
| 输出 | 您好，感谢您的咨询。针对您的情况，我为您提供以下解决方案：<br><br>**如果您能接收原手机号的验证码：**<br>请直接点击登录页【忘记密码】...<br><br>**如果您的原手机号已停用或无法接收验证码：**<br>由于涉及手机号更换和密码重置...建议您联系我们的在线客服... | 您好，感谢您的耐心咨询。由于您同时涉及手机号更换和密码遗忘，建议您先联系在线客服人工核实身份，我们的客服人员会帮您重新绑定手机号并重置密码，这样能更快地恢复账户访问。 |
| 差异 | 用markdown加粗做条件分支，四段，更像帮助文档 | 一段话给出最终结论（既然手机号已经换了，直接建议走人工核实），更符合真实客服会怎么打字回复 |

### Case 3：多轮追问（conv_003 后续，带历史）

用户消息：`那如果我再买一件20块的凑够200，这张券还能用吗？`（历史：Case 1 的第一轮对话）

| | V1 | V2 |
|---|---|---|
| 输出 | 您好！如果您再购买一件20元的商品，订单总额就能达到200元，这样就满足了满200减30优惠券的使用门槛，该券就可以正常使用了。<br><br>不过需要提醒的是，同一订单内通常只能使用一张满减优惠券...（追加了一段用户没问的叠加规则提醒） | 您好，感谢您的咨询。如果您再购买20元的商品，订单总金额就能达到200元，满足了满200减30优惠券的使用门槛，这张券就可以正常使用了。请在结算时选择该优惠券即可享受优惠。 |
| 差异 | 两版都正确利用了历史算对了180+20=200，V1额外追加了一段用户没问的"叠加规则"提醒，属于过度延伸 | 只回答用户实际问的问题，更简洁 |

## 结论

V2 在三个 case 上都更符合"客服在聊天窗口里应该怎么回复"的直觉：更短、格式更朴素、
在需要代入数字计算的场景里给出的结论更具体。V1 的内容并没有错误或编造，只是啰嗦、
格式偏文档化、有时会加上用户没问的额外建议。因此把 V2 设为 `generate_suggestion` 的默认版本，
V1 保留在代码里供随时回归对比。
